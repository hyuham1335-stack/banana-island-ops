# 파일럿 기록 (Pilot Log)

> **이 문서의 위치**
> 하네스 자체의 문서다. `docs/` 직속 7종(프로젝트가 채우는 자리)과는 층이 다르다.
> [ROADMAP.md](ROADMAP.md) 2단계의 승격 조건이 이 문서이며, 여기 적힌 실측이
> `calibrate` 구현과 어댑터 `verified` 승격 판단의 **유일한 근거**다.
> **추정치를 적지 않는다. 재보지 않은 것은 「미측정」으로 남긴다.**

> **이 파일은 비어 있다.** 템플릿을 만든 파일럿의 런 기록은 추출 이력(`git log`)에
> 있고, 팁에서는 걷어냈다 — 남의 실측을 상속하지 않기 위해서다
> ([ADR-H039](DECISIONS.md)). 아래는 **한 런이 무엇을 남겨야 하는가**의 형식이고,
> 첫 런이 그 아래에 자기 절을 연다.

---

## 무엇을 여기 적는가

**런 하나 = 절 하나.** 런이 끝난 직후에 적고, 나중에 기억으로 채우지 않는다.

| 적는 것 | 적지 않는 것 |
|---|---|
| 기계가 낸 숫자 (소요·테스트 수·재시도·종료 코드) | 어림·기억·"대략 이 정도였다" |
| 사람이 개입한 자리와 **왜** 개입했나 | 개입을 뭉갠 "완주했다" |
| 예측을 **먼저** 적고 결과와 대조한 표 | 사후에 맞춘 해석 |
| 실패(시스템이 못 함)와 결과 없음(데이터가 없음)을 **가른** 서술 | 둘을 같은 칸에 넣은 요약 |

**스킵은 통과가 아니다.** 못 잰 스테이지는 「미측정」으로 남기고, 그 값에서
유도되는 정책이 보수적 기본값으로 간다는 사실을 함께 적는다.

---

## 런 절의 형식

새 런은 아래 골격을 복사해 채운다. **빈 칸을 지우지 말고 「미측정」으로 남긴다** —
칸이 사라지면 재본 적 없다는 사실도 같이 사라진다.

```markdown
## 파이프라인 런 P{N} — `{기능 이름}` ({날짜})

| 항목 | 값 |
|------|-----|
| 일자 | |
| 런 ID | `_workspace/runs/{run_id}` |
| 브랜치 | (base ) |
| 대상 | (계약의 유닛·진입점) |
| 어댑터 | (`verified: ` ) |
| 결과 | 등급 · 종료 코드 |
| 머신 | OS · python · 러너 · 로캘 |

### 스테이지 실측

| 스테이지 | 소요 | 종료 코드 | 테스트 | 비고 |
|---|---:|---:|---:|---|

### 이 런이 확인하기로 했던 것 — 그리고 결과

**런 전에 적은 예측**과 실측을 나란히 둔다. 예측을 먼저 적었기 때문에 이 대조가
사후 합리화가 아니라 채점으로 남는다.

### 막힌 지점 · 수동 개입

파이프라인이 멈춰 사람에게 물은 자리. **상태를 잠갔는지**까지 적는다.

### 이 런이 연 하네스 결함

| ID | 무엇 | 상태 |
|---|---|---|

### 이 런이 승격 판단에 주는 답

[ROADMAP.md](ROADMAP.md) §6 의 「검증된 것과 아직 아닌 것」 표 중 어느 줄이
움직였나. **안 움직였으면 안 움직였다고 적는다.**

### 다음 런에서 볼 것
```

---

## 런 기록

<!-- 첫 런이 여기에 자기 절을 연다. -->
## 파이프라인 런 P1 — `{기능 이름}` (예측 초안 2026-09-19 · 런 전)

> **런 전에 적은 절이다.** 요청이 정해지면 `대상`·레인 가정을 채우고, 런이 끝나면
> 아래 표의 「결과」 칸과 나머지 절을 채운다. 예측 문구는 런 뒤에 고치지 않는다.

| 항목 | 값 |
|------|-----|
| 일자 | 미정 |
| 런 ID | 미정 |
| 브랜치 | 미정 (base `main` e0f8d19 — 템플릿 H056~H067 동기화 직후) |
| 대상 | 미정 |
| 어댑터 | `nextjs-ts` (`verified: true` — 2026-09-19 `verify-adapter`, 완주 15런 전부 00~08 passed) |
| 결과 | 미측정 |
| 머신 | Windows 10 · python 3 · npm · cp949 |

### 캘리브레이션 (2026-09-19 22:58, 런 전 재측정)

| 스테이지 | 소요 | 테스트 | 비고 |
|---|---:|---:|---|
| compile | 23.42s | — | `npm run typecheck` |
| lint | 46.06s | — | |
| check | 11.23s | — | `npm audit --audit-level=high` |
| scoped | 1.44s | — | |
| full | 10.91s | 682 | 백그라운드 OFF (임계 180s) · 타임아웃 300s |
| e2e | 미측정 | — | `cmd: null` → `stage_absent:e2e` |
| build | 45.25s | — | |
| docs | 미측정 | — | `cmd: null` |

유도: `tests_ran_floor` 14 → **613**. `retry_budget` 미측정(attempts 기록 0 step).

### 이 런이 확인하기로 했던 것 — 그리고 결과

기준선은 이 리포의 직전 12런(`20260915-0930-3b43` ~ `20260917-0028-40dc`, `normal` 10 · `small` 2)의
`state.json` 에서 뽑았다. 레인은 **`normal` 을 가정**한다 — `small` 이면 2·3 은 해당 없음으로 적는다.

| # | 예측 | 기준선 · 근거 | 볼 곳 | 결과 |
|---|---|---|---|---|
| 1 | 등급은 `PASS_WITH_GAPS` 이고 gap 은 `stage_absent:e2e` · `stage_absent:docs` 둘이 전부 — `adapter_unverified` 는 사라진다 | 완주한 11런 모두 이 둘 + `adapter_unverified` 를 가짐 (`e9d6` 은 미완주). 어댑터는 런 전에 `verified: true` 로 올렸다 | `state.gaps` | |
| 2 | 테스트 파일이 바뀌면 `test` 리뷰어는 떨어지지 않는다. 다섯이 다 매칭되면 `routing.dropped` 는 `[arch]` | 직전 12런 중 10런이 `test` 를 떨어뜨림 (ADR-H062) | 05 `routing.dropped` | |
| 3 | `src/services/**` 를 건드리면 `data` 가 매칭되고, 01 의 `risk` 에 `schema`·`boundary` 가 없으면 `risk_undeclared: ["data"]` 가 남는다 | 기준선 없음 — 새 관측 (ADR-H067) | 이벤트 `risk_undeclared` · 05 노드 | |
| 4 | `risk` 가 비지 않아 02 는 돈다 (`no_risk` 생략 안 됨) | 직전 12런 모두 02 가 돌았음 (xv primary 11 · fallback 1) | 02 `status` · 생략 사유 | |
| 5 | `precheck` 가 **파일 수로** exit 9 를 내지 않는다 | 직전 at_05 파일 수 6~23, 10 초과 6런 — 테스트 포함 셈 (ADR-H066) | `precheck.budget` · `test_files_excluded` | |
| 6 | `budget.model_calls.total` 은 12~20 이고 상한 24 안 | 완주 11런 9~19 (중앙 12). 05 수리가 이제 계수에 들어가 늘 수 있다 (ADR-H064) | `budget.model_calls` | |
| 7 | 04 수리 ≤ 1회 · 05 수리(`review_repair`) ≤ 2회 — 작성자가 전 레인 sonnet 이어도 | 직전 04 수리 0~2 · 05 수리 0~2 (ADR-H061) | `counters.repair` · `counters.review_repair` · `state.models.instructed` | |
| 8 | 요청이 화면을 건드리면 계약에 `## 화면` 이 생기고 03 이 `ui-writer` 를 부른다. `src/components/**` 변경이 impl 소유 위반으로 걸리지 않는다 | 새 역할 — 기준선 없음 (ADR-H057) | 03 디스패치 · 소유 검사 | |
| 9 | 런 비용 — **예측하지 않는다.** 첫 값이 기준선이다. 세션을 닫은 뒤 `cli.py cost` 로 잰다 | 원장이 이 런부터 쌓임 | `cli.py cost --run-id` | |

### 막힌 지점 · 수동 개입

미측정

### 이 런이 연 하네스 결함

| ID | 무엇 | 상태 |
|---|---|---|

### 이 런이 승격 판단에 주는 답

미측정

### 다음 런에서 볼 것

미측정

## 파이프라인 런 P2 — `should-fr024-sheet-webhook` (2026-09-20)

| 항목 | 값 |
|------|-----|
| 일자 | 2026-09-20 |
| 런 ID | `_workspace/runs/20260919-2343-d42d` |
| 브랜치 | `feat-fr024-sheet-webhook` |
| 대상 | 런 보고서 `docs\harness\pipeline\runs\20260919-2343-d42d.md` 의 계약 절을 본다 (계약은 06 에서 지워진다) |
| 어댑터 | `nextjs-ts` (`verified: true`) |
| 결과 | PASS_WITH_GAPS · gaps 4 (triage_miss:01:max_rounds=2, stage_absent:e2e, stage_na:docs, instruction_slot_over_budget) |
| 머신 | 미측정 |

### 스테이지 실측

| 스테이지 | 소요 | 종료 코드 | 테스트 | 비고 |
|---|---:|---:|---:|---|
| 00-triage | 0:00:37 | — | — | 페이즈 벽시계 · 순 작업 0:00:37 · 대기 — |
| 01-plan | 0:03:53 | — | — | 페이즈 벽시계 · 순 작업 0:03:53 · 대기 — |
| 02-cross-verify | 0:04:48 | — | — | 페이즈 벽시계 · 순 작업 0:04:48 · 대기 — |
| 03-implement | 0:05:08 | — | — | 페이즈 벽시계 · 순 작업 0:05:08 · 대기 — |
| 04-gate | 0:10:17 | — | — | 페이즈 벽시계 · 순 작업 0:10:17 · 대기 — |
| 05-code-review | 0:04:20 | — | — | 페이즈 벽시계 · 순 작업 0:04:18 · 대기 0:00:02 |
| 06-pr | 0:05:50 | — | — | 페이즈 벽시계 · 순 작업 0:01:18 · 대기 0:04:32 |
| 07-pr-review | 0:01:11 | — | — | 페이즈 벽시계 · 순 작업 0:01:11 · 대기 — |
| 테스트 | — | — | 700 | ok |

### 이 런이 확인하기로 했던 것 — 그리고 결과

미측정 — 런 전에 적은 예측이 상태에 없다. 런 보고서 `docs\harness\pipeline\runs\20260919-2343-d42d.md` 의 서술을 본다.

### 막힌 지점 · 수동 개입

에스컬레이션 0회 · 사람 판단 대기 0:04:34 · 형식 반려 1회

### 이 런이 연 하네스 결함

| ID | 무엇 | 상태 |
|---|---|---|
| — | 미측정 — 사람이 적는다 | |

### 이 런이 승격 판단에 주는 답

승격된 규칙 없음 — ROADMAP §6 표는 움직이지 않았다.

### 다음 런에서 볼 것

런 보고서 `docs\harness\pipeline\runs\20260919-2343-d42d.md` 의 「다음 런에서 바꿀 것」.

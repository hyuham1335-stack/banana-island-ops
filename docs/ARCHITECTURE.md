# 아키텍처

<!-- 이 문서는 **빈 골격**이다. 하네스를 돌리기 전에 채운다 —
     문서가 하네스 설정의 입력이기 때문이고, 무엇이 무엇의 입력인지는
     `docs/harness/ROADMAP.md` §4 의 표에 있다.
     절 제목은 지우지 말고 **빈 절로 남긴다** — 칸이 사라지면
     안 정했다는 사실도 같이 사라진다. -->

## 작성 규칙

<!-- 구조·흐름은 **항상 Mermaid** 로 그린다. 어떤 다이어그램을 언제 쓰는지 표를 여기 둔다 -->

## 디렉토리 구조

<!-- 역할별 소유 경계 glob(`harness/config.json` 의 `roles[].owns`)이 여기서 나온다 -->

## 레이어 의존 관계

<!-- **무엇이 무엇을 부를 수 있는가.** 방향이 한 줄이라도 뒤집히면 게이트가 잡아야 한다 -->

## 패턴

<!-- 이 리포에서 반복되는 형태와 그것을 고른 이유 -->

## 데이터 흐름

<!-- 요청 하나가 어디를 거쳐 무엇이 되는가 -->

## 상태 관리

<!-- 서버 상태와 클라이언트 상태를 가른다 -->
